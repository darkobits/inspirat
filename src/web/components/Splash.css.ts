import { style } from '@vanilla-extract/css';

export default {
  splash: style({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'column',
    height: '100%',
    width: '100%',
    cursor: 'initial'
  })
};
